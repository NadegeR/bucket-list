<nav>
    <ul>
        <li><a href="{{ path('main_home') }}" title="Back Home">Home</a></li>
        <li><a href="{{ path('wish_list') }}" title="All wishes">All wishes</a></li>
        <li><a href="{{ path('main_aboutUs') }}" title="About us">About Us</a></li>
        {% if app.user %}
        <li><a href="{{ path('wish_create') }}" title="Create your wish">Add yours !</a></li>
        <li class="nav-right"><a href="{{ path('app_logout') }}" title="logout">Logout ( {{ app.user.username }}
                )</a></li>
        {% else %}
            <li class="nav-right"><a href="{{ path('app_login') }}" title="login">Login</a> | <a
                        href="{{ path('app_register') }}" title="register">Register</a></li>
        {% endif %}
    </ul>
</nav>
